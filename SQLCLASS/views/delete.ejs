<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Delete User</title>

    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
    />

    <style>
      body {
        margin: 0;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f4f6f8;
        color: #1f2937;
      }

      header {
        background-color: #ef4444;
        color: white;
        padding: 18px 30px;
        font-size: 22px;
        font-weight: 600;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      }

      main {
        display: flex;
        justify-content: center;
        align-items: flex-start;
        padding: 40px 20px;
      }

      .card {
        background-color: white;
        width: 100%;
        max-width: 520px;
        padding: 30px;
        border-radius: 12px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
      }

      .warning {
        background: #fef2f2;
        border: 1px solid #fecaca;
        color: #991b1b;
        padding: 12px;
        border-radius: 8px;
        margin-bottom: 16px;
        font-size: 14px;
      }

      label {
        font-weight: 600;
        margin-top: 10px;
      }

      input {
        width: 100%;
        margin-top: 6px;
        padding: 10px;
        border-radius: 6px;
        border: 1px solid #d1d5db;
        font-size: 14px;
      }

      input:focus {
        outline: none;
        border-color: #ef4444;
        box-shadow: 0 0 0 2px rgba(239, 68, 68, 0.2);
      }

      .btn-group {
        display: flex;
        gap: 10px;
        margin-top: 20px;
      }
    </style>
  </head>

  <body>
    <header>Delete User</header>

    <main>
      <div class="card">
        <div class="warning">
          <strong>Warning:</strong> This action is permanent. Enter the user’s
          password to confirm deletion.
        </div>

        <h5 class="mb-3">
          Delete this user: <strong><%= user.EMAIL %></strong>
        </h5>

        <form action="/user/<%= user.ID %>?_method=DELETE" method="post">
          <label>Password</label>
          <input
            type="password"
            name="password"
            placeholder="Enter password to delete"
            required
          />

          <div class="btn-group">
            <button type="submit" class="btn btn-danger">Delete</button>
            <a href="/user" class="btn btn-secondary">Cancel</a>
          </div>
        </form>
      </div>
    </main>
  </body>
</html>